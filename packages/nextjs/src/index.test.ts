import { describe, it, expect } from "vitest";

describe("NextJS Package", () => {
  it("should be importable", () => {
    // Basic test to ensure the package structure is correct
    expect(true).toBe(true);
  });
});
